# 贝叶斯概率公式（Bayes' Theorem）

## 一、基本公式

```markdown
P(A|B) = \frac{P(B|A)\cdot P(A)}{P(B)}
```

---

## 二、各符号含义

* `P(A|B)`：在事件 B 发生的条件下，事件 A 发生的概率（后验概率）
* `P(B|A)`：在事件 A 发生的条件下，事件 B 发生的概率（似然）
* `P(A)`：事件 A 的先验概率
* `P(B)`：事件 B 的边缘概率（归一化因子）

---

## 三、展开形式（全概率公式）

```markdown
P(B) = \sum_i P(B|A_i)\cdot P(A_i)
```

代入可得：

```markdown
P(A_k|B) = \frac{P(B|A_k)\cdot P(A_k)}{\sum_i P(B|A_i)\cdot P(A_i)}
```

---

## 四、结构化认识

```markdown
后验 = (似然 × 先验) / 证据
```

---

## 五、示例

```markdown
已知：
P(有病) = 0.01
P(阳性 | 有病) = 0.99
P(阳性 | 无病) = 0.01

求：
P(有病 | 阳性)

解：
P(阳性) = 0.99×0.01 + 0.01×0.99 = 0.0198

P(有病 | 阳性) = (0.99×0.01) / 0.0198 ≈ 0.5
```

---

## 六、总结

```markdown
贝叶斯公式 = 用新证据动态修正原有认知的数学规则
```
